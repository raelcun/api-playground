Core:
  Libs:
    Config:
      [] rework config loading to validate config after loading from all sources (files, env vars, etc) so it doesn't fail when there aren't env vars
      Basic Validation:
        [] ensure generic usage
        [] ensure dependencies are injected
        [] ensure sufficient logging
        [] ensure full test suite
    Enforce Header Role Middleware:
      Basic Validation:
        [] ensure generic usage
        [] ensure dependencies are injected
        [] ensure sufficient logging
        [] ensure full test suite
    Http Client:
      Basic Validation:
        [] ensure generic usage
        [] ensure dependencies are injected
        [] ensure sufficient logging
        [] ensure full test suite
    Logging Middleware:
      Basic Validation:
        [] ensure generic usage
        [] ensure dependencies are injected
        [] ensure sufficient logging
        [] ensure full test suite
    Rate Limiter:
      Basic Validation:
        [] ensure generic usage
        [] ensure dependencies are injected
        [] ensure sufficient logging
        [] ensure full test suite
    RBAC:
      Basic Validation:
        [] ensure generic usage
        [] ensure dependencies are injected
        [] ensure sufficient logging
        [] ensure full test suite
    Response Validator Middleware:
      Basic Validation:
        [] ensure generic usage
        [] ensure dependencies are injected
        [] ensure sufficient logging
        [] ensure full test suite
    Test Utils:
      Basic Validation:
        [] ensure generic usage
        [] ensure dependencies are injected
        [] ensure sufficient logging
        [] ensure full test suite
    Utils:
      Basic Validation:
        [] ensure generic usage
        [] ensure dependencies are injected
        [] ensure sufficient logging
        [] ensure full test suite
      
Basic Task Management:
  Task Service:
    [] edit task
    [] get task
    [] get all tasks
  Task Routes:
    [] edit task route
    [] get task route
    [] get all tasks route
Archive:
  ✔ ensure generic usage @started(20-06-21 15:13) @done(20-06-21 15:14) @lasted(1m31s) @project(Core.Libs.Dynamo Client.Basic Validation)
  ✔ ensure dependencies are injected @done(20-06-21 15:14) @project(Core.Libs.Dynamo Client.Basic Validation)
  ✔ ensure full test suite @done(20-06-21 19:44) @project(Core.Libs.Dynamo Client.Basic Validation)
  ✔ ensure generic usage @done(20-06-21 12:53) @project(Core.Libs.Body Validator.Basic Validation)
  ✔ ensure dependencies are injected @done(20-06-21 12:53) @project(Core.Libs.Body Validator.Basic Validation)
  ✔ ensure sufficient logging @done(20-06-21 12:53) @project(Core.Libs.Body Validator.Basic Validation)
  ✔ ensure full test suite @done(20-06-21 12:53) @project(Core.Libs.Body Validator.Basic Validation)
  ✔ ensure sufficient logging @started(20-06-21 00:59) @done(20-06-21 15:07) @lasted(14h8m16s) @project(Core.Libs.Dynamo Client.Basic Validation)
  ✔ ensure generic usage @done(20-06-20 20:40) @project(Core.Libs.Emergency Logger.Basic Validation)
  ✔ ensure dependencies are injected @done(20-06-20 20:40) @project(Core.Libs.Emergency Logger.Basic Validation)
  ✔ ensure sufficient logging @done(20-06-20 20:40) @project(Core.Libs.Emergency Logger.Basic Validation)
  ✔ ensure full test suite @done(20-06-20 20:40) @project(Core.Libs.Emergency Logger.Basic Validation)
  ✔ ensure generic usage @done(20-06-20 20:39) @project(Core.Libs.Error.Basic Validation)
  ✔ ensure dependencies are injected @done(20-06-20 20:39) @project(Core.Libs.Error.Basic Validation)
  ✔ ensure sufficient logging @done(20-06-20 20:39) @project(Core.Libs.Error.Basic Validation)
  ✔ ensure full test suite @done(20-06-20 20:39) @project(Core.Libs.Error.Basic Validation)
  ✔ ensure generic usage @done(20-06-21 15:09) @project(Core.Libs.Logger.Basic Validation)
  ✔ ensure dependencies are injected @done(20-06-21 15:09) @project(Core.Libs.Logger.Basic Validation)
  ✔ ensure sufficient logging @done(20-06-21 15:09) @project(Core.Libs.Logger.Basic Validation)
  ✔ ensure full test suite @done(20-06-21 15:09) @project(Core.Libs.Logger.Basic Validation)
  ✔ standardized response format @started(20-06-18 22:02) @done(20-06-19 02:21) @lasted(4h19m21s) @project(Core.Libs.Logger)
  ✔ standardized log builder @done(20-06-20 19:17) @project(Core.Libs.Logger)
  ✔ standardized log format @done(20-06-20 19:16) @project(Core.Libs.Logger)
  ✔ dynamo client @done(20-06-17 21:25) @project(Basic Task Management.Task Service)
  ✔ add task @done(20-06-17 21:26) @project(Basic Task Management.Task Service)
  ✔ remove task @done(20-06-17 21:26) @project(Basic Task Management.Task Service)
  ✔ add task route @project(Basic Task Management.Task Routes)
  ✔ delete task route @started(20-06-17 22:27) @done(20-06-17 23:16) @lasted(49m14s) @project(Basic Task Management.Task Routes)
